{
  "name": "wikipedia-graph",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm:dev --prefix server\" \"npm:dev --prefix client\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write . --ignore-path .gitignore --ignore-path ./client/.gitignore --ignore-path ./wikidump-importer/.gitignore"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "prettier": "^3.8.1"
  }
}
